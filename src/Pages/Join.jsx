// import { useState, useContext } from "react";
// import { toast } from "react-toastify";

// import { AuthContext } from "../context/AuthContext";

const Join = () => {


    return (
        <form className="flex flex-col gap-4">
            <button
                type="submit"

                className={`btn bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-700 transition  ? "opacity-50 cursor-not-allowed" : ""
                    }`}
            >
            </button>
        </form>
    );
};

export default Join;
